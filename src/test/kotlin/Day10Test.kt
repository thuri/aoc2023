import org.junit.jupiter.api.Named
import org.junit.jupiter.params.provider.Arguments
import java.util.stream.Stream

class Day10Test {


  companion object {
    @JvmStatic
    fun part1Input() : Stream<Arguments> {
      return Stream.of(
        Arguments.of(Named.of("example", ))
      )
    }
  }
}